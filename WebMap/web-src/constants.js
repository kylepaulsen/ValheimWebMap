const constants = {
	CANVAS_WIDTH: 2048,
	CANVAS_HEIGHT: 2048,
	PIXEL_SIZE: 12,
	EXPLORE_RADIUS: 100,
	DEFAULT_ZOOM: 100
};

constants.COORD_OFFSET = constants.CANVAS_WIDTH / 2;

export default constants;
